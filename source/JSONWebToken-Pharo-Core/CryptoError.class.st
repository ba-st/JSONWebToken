"
In GS64, an exception signaled when certain encrypt/decrypt methods fail..

This version is just a placeholder so we can easily create extensions methods to load in GS64 specific
packages.
"
Class {
	#name : #CryptoError,
	#superclass : #Error,
	#category : #'JSONWebToken-Pharo-Core'
}
